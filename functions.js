function greet(name, weather) {
    console.log(`Hello, ${name}!`);
    console.log(`It's ${weather} today right?`);
}
greet("Alice", "rainy");

function sum (a, b) {
    return a +b;
}
let result = sum(3, 5);
console.log(result);